# OpenAI API配置示例
# 请复制此文件为 .env 或直接设置环境变量

# OpenAI API密钥（必需）
export OPENAI_API_KEY=sk-u8uCPPzw8TXmcBHJ1pMOaBlI27AJMO4zlREVc4oNeBkHExLE

# API端点和模型配置
export OPENAI_API_BASE=https://turingai.plus/v1/chat/completions
export OPENAI_MODEL=gpt-4o-mini
export OPENAI_MAX_TOKENS=500
export OPENAI_TEMPERATURE=0.7

# 服务器配置（可选）
export STATIC_SERVER_PORT=8000
export API_SERVER_PORT=8001

# 使用方法：
# 1. 复制此文件：cp env.example .env
# 2. 编辑 .env 文件，填入真实的API密钥
# 3. 在启动服务器前运行：source .env
# 4. 然后运行：python3 server.py

# 安全提示：
# - 不要将真实的API密钥提交到版本控制系统
# - 建议将 .env 添加到 .gitignore 中
